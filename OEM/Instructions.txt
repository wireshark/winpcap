Add the following lines to Win32-Extensions.c:

int pcap_start_oem(char** err_str)
{
	if(PacketStartOem(err_str) == 0)
	{
		return -1;
	}
	return 0;
}

---------------------------------

Add the following line to the wpcap defs:

pcap_start_oem

---------------------------------